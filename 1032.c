#include<stdio.h>
int main(){
    int n;
    char c[50][50];
    scanf("%d", &n);
    for(int i=0; i<n; i++){
        scanf("%s", c[i]);
        for(int j=0; j<sizeof(c[i]); j++){
            if()
        }
    }
    return 0;
}